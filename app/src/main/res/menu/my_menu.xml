<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- "Mark Favorite", must appear as action button if possible. -->
    <item
        android:id="@+id/action_new"
        android:icon="@drawable/baseline_create_new_folder"
        android:title="New"
        app:showAsAction="never"/>

    <item
        android:id="@+id/action_favorite"
        android:icon="@drawable/like"
        android:title="Favorite"
        app:showAsAction="never"/>

    <item
        android:id="@+id/action_settings"
        android:icon="@drawable/baseline_settings"
        android:title="Settings"
        app:showAsAction="never"/>

    <item
        android:id="@+id/action_search"
        android:icon="@drawable/baseline_search"
        android:title="Search"
        app:actionViewClass="com.google.android.material.search.SearchBar"
        app:showAsAction="always"/>
</menu>